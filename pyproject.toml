[build-system]
requires = ["setuptools", "wheel", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "videodec"
version = "0.1.0"
description = "MEGA AES-CTR Decryption Engine"